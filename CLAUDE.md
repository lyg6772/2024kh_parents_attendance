# CLAUDE.md

@AGENTS.md

> Claude Code 는 `CLAUDE.md` 를 자동으로 읽고 `AGENTS.md` 는 읽지 않는다. 이 파일은
> `AGENTS.md` 의 하네스 규칙 — TDD·테스트 잠금, 기본 브랜치 푸시 금지, 리뷰-수정
> 루프 상한 — 이 누군가 읽기로 선택하는 것에 기대지 않고 실제로 컨텍스트에 들어오게
> 하려고 존재한다. `@` import 가 그 장치이고, "AGENTS.md 를 읽어라"라는 문장은 산문일
> 뿐이다.
>
> 이 파일은 항상 로드되므로, 내용은 `AGENTS.md` 한 곳이 소유한다. 여기에는 로딩에
> 관한 것 말고 아무것도 더하지 않는다.
